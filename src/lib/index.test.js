// Import the module
import { CometChatUI } from "./index";

// Define the test suite
describe("CometChatUI", () => {
  // Define a single test
  it("should be defined", () => {
    // Check if CometChatUI is defined
    expect(CometChatUI).toBeDefined();
  });
});
